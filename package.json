{
  "name": "acnh-turnip-predictor",
  "private": true,
  "version": "0.02.0",
  "description": "動物森友會大頭菜價格預測器 - 純演算法版預測引擎",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "build": "node build.mjs",
    "serve": "npx --yes serve . -l 5173"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "esbuild": "^0.28.0",
    "typescript": "^5.6.3",
    "vitest": "^5.0.0"
  }
}
